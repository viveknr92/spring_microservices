FROM maven:3.5.2-jdk-8-alpine AS MAVEN_TOOL_CHAIN
COPY pom.xml /tmp/
COPY src /tmp/src/
WORKDIR /tmp/
CMD ["mvn", "clean", "install"]

FROM openjdk:8-jre-alpine
WORKDIR /tmp/
COPY --from=0 target/MovieInfoService-0.0.1-SNAPSHOT.jar /app.jar
CMD ["java", "-jar", "/app.jar"]